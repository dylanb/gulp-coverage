var assert = require('assert'),
    test = require('./chain');

describe('Test Src', function () {
    it('Should run this test Src', function () {
        test();
        assert.ok(true);
    });
});
